<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happyhome.rental.email.dao.RentalNoticeEmailLogMapper">
    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM rental_notice_email_logs
        WHERE user_id = #{userId}
          AND notice_id = #{noticeId}
          AND event_type = #{eventType}
    </select>

    <insert id="save">
        INSERT IGNORE INTO rental_notice_email_logs (
            user_id,
            notice_id,
            event_type,
            recipient_email,
            subject
        ) VALUES (
            #{userId},
            #{noticeId},
            #{eventType},
            #{recipientEmail},
            #{subject}
        )
    </insert>
</mapper>
