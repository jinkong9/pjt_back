<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happyhome.analysis.dao.AnalysisSnapshotMapper">
    <insert id="insert">
        INSERT INTO analysis_snapshot (
            label, latitude, longitude, radius_m, commercial_count, traffic_event_count,
            score, risk_level, source, created_at
        ) VALUES (
            #{label}, #{latitude}, #{longitude}, #{radiusMeters}, #{commercialCount},
            #{trafficEventCount}, #{score}, #{riskLevel}, #{source}, CURRENT_TIMESTAMP
        )
    </insert>
</mapper>

